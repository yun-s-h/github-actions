site_name: spiderpool
docs_dir: docs
site_dir: site
repo_name: yun-s-h/github-actions
repo_url: https://github.com/yun-s-h/github-actions
remote_branch: main
theme:
  name: readthedocs
  # The maximum depth of the navigation tree in the sidebar
  navigation_depth: 2
  palette:
    scheme: default
  features:
    - navigation.instant
    - navigation.top
    - navigation.tracking
    - search.highlight
    - search.suggest
    - search.share

plugins:
  - tags
  - search

markdown_extensions:
  - meta
  - toc
  - tables
  - fenced_code
  - tables
  - attr_list
